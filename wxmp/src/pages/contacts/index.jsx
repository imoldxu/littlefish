import { ScrollView } from "remax/wechat"
import useTourist from "../../hooks/tourist"

export default ()=>{

    const tourist = useTourist()

    return(
        <ScrollView className="x-page">

        </ScrollView>
    )
}